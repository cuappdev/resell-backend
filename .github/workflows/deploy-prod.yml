# DISABLED: Docker Swarm / DigitalOcean deploy retired in favor of Vercel + Neon.
# Kept for reference only. Do not re-enable without removing Vercel production.
name: "[disabled] Docker deploy resell-prod"

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: |
          echo "This workflow is disabled. Deploy via Vercel; migrate via migrate-neon.yml."
          exit 1
